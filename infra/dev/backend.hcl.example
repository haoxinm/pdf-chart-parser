# Copy to backend.hcl (git-ignored) and fill in your values.
# Then run: infra/setup.sh dev

bucket       = "my-terraform-state-bucket"
key          = "pdf-chart-parser/dev/terraform.tfstate"
region       = "us-west-2"
use_lockfile = true
